Pick the next bead related to crawling and incorporating data about sheet music for flute and work on it. When you finished working on the bead, commit and push.

Completed when:
- The UI has the new fields described in the beads. 
- The data is crawled and incorporated into the app.
- For all data that is missing for instance the Level or the Instrumentation, add the ability to add this information via the UI.
- If there is a conflict, because the new data has a piece that already exists in the app, go with the new data.
- All tests pass.
- Output: <promise>COMPLETE</promise>