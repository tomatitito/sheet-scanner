Build a new input field into the UI that allows for saving "source" for the sheet music that is added to the catalog. For instance, a source might be "private", if the sheet music is owned privately. Or it might be "music school" for the school where a teacher works. 

Completed when:
- The UI has a new input field for the source. 
- The values for this field can be selected via a drop-down menu. 
- One value named "private" is already configured and available via the drop-down menu. 
- There is an option for the user to add more values for the source field. 
- These newly added values should also be selectable via the drop-down menu once they are configured. 
- The new feature is tested. 
- The new feature is also documented. 
- Output: <promise>DONE</promise>